/**
 * State Machine Module
 *
 * Exports game state machine and related types.
 */

export { GameStateMachine, InvalidTransitionError } from './GameStateMachine';
export { GameState, GameEvent, StateTransitionTable } from './types';
